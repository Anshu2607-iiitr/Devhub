version: '3.8'

services:
  mplads-vigil:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mplads-vigil-platform
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    restart: unless-stopped
